{% extends 'base.html.twig' %}

{% block title %}Category
{% endblock %}

{% block body %}



<div class="container">
	<div class="card rounded" style="width: 20rem;">
		<div class="card-body">
			<h5 class="card-title">{{ category.name }}</h5>
			<h6 class="card-subtitle mb-2 text-muted">l'id n° {{ category.id }}</h6>
			
			<button type="button" class="btn btn-outline-dark mb-2">
		        <a href="{{ path('app_admin_category_index') }}">retour à la list</a>
	        </button>
			<button type="button" class="btn btn-outline-dark mb-2">
		        <a href="{{ path('app_admin_category_edit', {'id': category.id}) }}">Modifier</a>
	        </button>
            
                {{ include('admin_category/_delete_form.html.twig') }} 
            
		</div>
	</div>
</div> 
 
{% endblock %}
