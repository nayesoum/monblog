{% extends 'base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}


	<div class="container">
		<div class="container ">
			<div class="p-5 mb-4 bg-light d-flex justify-content-center rounded-3">
				<div class="container-fluid py-5">
					<h1 class="display-5 fw-bold mb-5">Mon espace
					</h1>
					<p class=" fs-4 ">Pour modifier un utilisteur je dois faire preuvent d'une grande vigilance
						<br>c'est n'est pas un jeu alors attention.
						<br>
						Tous abus sera sanction<br>sous peine d'etre retiré et les utilisateurs supprimer.</p>

					<div class="mx-4 ">
						<button type="button" class="btn btn-outline-dark mt-2">
							<a href="{{ path('app_admin_user_index') }}">retour a la liste d'utilisateur</a>
						</button>
					</div>
				</div>

				<div class="card my-5 pt-5 Regular shadow" style="width: 18rem;">
					<div class="card-body">

						<h5 class="card-subtitle mb-2">Pseudo :{{ user.pseudo }}</h5>
						<p class="card-text">Email :{{ user.email }}</p>

						<button type="button" class="btn btn-outline-dark my-3 ms-4">
							<a href="{{ path('app_admin_user_edit', {'id': user.id}) }}">modifier</a>
						</button>
						<div class="mx-4 py-3">
							{{ include('admin_user/_delete_form.html.twig') }}
						</div>
					</div>
				</div>
			</div>
		{% endblock %}
