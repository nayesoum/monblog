{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <div class="card mb-3" style="max-width: 540px;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{ article.image }}" class="img-fluid rounded-start" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{ article.title }}</h5>
        <p class="card-text"> Article n {{ article.id }}</p>
        <p class="card-text"><small class="text-muted">{{ article.Content }}</small></p>
        <p class="card-text"><small class="text-muted">{{ article.Category }}</small></p>
       
      </div>
    </div>
  </div>
</div>

<div class="card mb-3">
  <img src="{{ article.image }}" class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">{{ article.title }}</h5>
    <p class="card-text"><small class="text-muted">{{ article.Category }}</small></p>
    <p class="card-text">{{ article.Content }}</p>
   
  </div>
</div>
    <a href="{{ path('app_admin_article_index') }}">Retour à la liste d'article</a>

    <a href="{{ path('app_admin_article_edit', {'id': article.id}) }}"><i class="bi bi-pencil-fill"></i></a>

    {{ include('admin_article/_delete_form.html.twig') }}
{% endblock %}
