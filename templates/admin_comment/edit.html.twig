{% extends 'base.html.twig' %}

{% block title %}Edit Comment
{% endblock %}

{% block body %}

	{# <div class="container">
				<div class="p-5 mb-4 bg-light rounded-3">
					<div class="container-fluid py-5">
						<h1 class="display-5 fw-bold">Mes commentaire
						</h1>
						<p class="col-md-8 fs-4">Pour voir tous les commentaire, les modifier et ou en créer de nouveaux.</p>
		
					</div>
				</div>
		
		
				<div class="mb-4">
					<button type="button" class="btn btn-outline-dark">
						<a href="{{ path('app_admin_category_new') }}">Creer une nouvelle categorie</a>
					</button>
				</div>
		
				{{ include('admin_comment/_form.html.twig', {'button_label': 'Update'}) }}
		
				<a href="{{ path('app_admin_comment_index') }}">retour a la liste de commentaire</a>
		
				{{ include('admin_comment/_delete_form.html.twig') }}
		
			</div> #}

{# 
	<div class="container">

		<div class="container-fluid py-5">
			<h1 class="display-5 fw-bold">Mes commentaire
			</h1>
			<p class="col-md-8 fs-4">Pour voir tous les commentaire, les modifier et ou en créer de nouveaux.</p>

		</div>


		<div class="card" style="width: 40rem;">

			<h5 class="display-5 fw-bold">Commentaire de
				{{ comment.user }}
			</h5>
			{{ include('admin_comment/_form.html.twig', {'button_label': 'Update'}) }}

			<button type="button" class="btn btn-outline-dark">
				<a href="{{ path('app_admin_comment_index') }}">retour a la liste de commentaire</a>
			</button>

			<div class="mt-3 mb-4">
				{{ include('admin_comment/_delete_form.html.twig') }}
			</div>

		</div>


	</div> #}

	<div class="container ">
		<div class="p-5 mb-4 bg-light d-flex justify-content-center rounded-3">
			<div class="container-fluid py-5">
				<h1 class="display-5 fw-bold mb-5">Mon espace
				</h1>
				<p class=" fs-4 ">Pour modifier un commentaire je dois faire preuvent d'une grande
					<br>
					determination ecrire un commentaire c'est pas rien 🤣.
					<br>
					Les commentaires doivent etre respecteux<br>sous peine d'etre retiré et l'utilisateur suprimer.</p>

				<div class="mx-4 ">
					<button type="button" class="btn btn-outline-dark">
						<a href="{{ path('app_admin_comment_index') }}">retour a la liste de commentaire</a>
					</button>
				</div>
			</div>

			<div class="card px-3 Regular shadow" style="width: 50rem;">
				<h3 class="text-center mt-5 mb-5">modifier un commentaire
										Commentaire de
					{{ comment.user }}</h3>

				<div class="mt-3 mb-4">
					{{ include('admin_comment/_form.html.twig', {'button_label': 'Update'}) }}
				</div>
				<div class="mt-3 mb-4">
					{{ include('admin_comment/_delete_form.html.twig') }}
				</div>

			</div>
		</div>
	</div>
{% endblock %}
